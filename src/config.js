// ---------------------------------------------------------------------------
// Admin login credentials.
//
// Change the username/password below to whatever you like, then save the file.
// (When deployed, you commit the change to GitHub and the site rebuilds.)
//
// IMPORTANT: this is a static, frontend-only site, so this login only HIDES the
// admin panel from casual visitors — it is not real security. A visitor cannot
// change your published listings though: their edits only affect their own
// browser. Real, server-saved admin would require a backend.
// ---------------------------------------------------------------------------
export const ADMIN_CREDENTIALS = {
  username: "admin",
  password: "premium2026",
};
